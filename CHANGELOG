0.8.1 (..)

* Fix bug with Windows build -- avoid large classpath which was exceeding the max line length on Windows
* Automatically assign a configuration when importing from a Jenkins matrix job
* Fix bug with adding executions incrementally to a batch
* Add ui.applicationName configuration property to allow customisation of the project name

0.8.0 (9/Nov/2014)

* Revamped test history charts into stacked bar charts with a bar per day
* Add larger version of each test history chart via an Expand button
* Support for test categories
* Record batches incrementally via API
* Performance improvements for batch import
* More compact filter widget on Tests Screen
* Improvements for chart tooltips
	
0.1.0 (22/Oct/2014)

* Initial release
